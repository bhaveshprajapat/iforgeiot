!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title IoT iForge Project
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_pack_tag 'application', 'data-turbolinks-track': 'reload'

  %body{}
    .flash-messages
      - flash.each do |name, msg|
        - next unless name == 'alert' || name == 'notice'
        .alert.fade.in
          .container
            - if name == 'notice'
              = fa_icon('check', class: 'flash-success')
            - else
              = fa_icon('warning', class: 'flash-error')
            = msg
            %a{ href: '#', title: 'Hide this message', data: { dismiss: :alert } } Dismiss


    %noscript
      .warning-page-cover
        .alert.alert-info
          %h2 Sorry, it appears that you are using a web browser without JavaScript which prevents us offering you a rich online experience.
          %p Please enable JavaScript or use a different web browser, or alternatively contact the CiCS Helpdesk for assistance.

    #wrap
      %nav#main-nav.navbar.navbar-default.navbar-fixed-top
        .container
          .navbar-header
            %button.navbar-toggle{data: { target: ".navbar-collapse", toggle: :collapse, type: :button }}
              %span.icon-bar
              %span.icon-bar

              %span.icon-bar
            = link_to :root, class: 'navbar-brand hidden-sm headertext' do
              %p iForge Machine Access Dashboard


          .collapse.navbar-collapse
            %ul.nav.navbar-nav.navbar-right
              %ul.nav.navbar-nav.navbar-right
                %li
                  = link_to '#', class: 'dropdown-toggle headertext', role: 'button' do
                    = fa_icon 'user', text: "Link"
                %li
                  = link_to '#', method: :get, title: "Change your password", :id => "changeadminpassword", :class => "headertext" do
                    = fa_icon 'edit', text: 'Link'
                %li
                  = link_to '#', method: :get, title: "Log out of the system", :class => "headertext" do
                    = fa_icon 'sign-out', text: 'Link'

      #main-container.container

        = yield

    %footer.footer#footer
      .container
        %p.footertext
          &copy;#{Date.today.year} Alexander Yates, Carlota Arvela, Bhavesh Prajapat, Amira Deana Abraham, Daniel Ryder

